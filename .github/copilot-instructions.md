# Copilot System Instructions

When executing tasks related to Flutter or Database Migration, ensure you use the following capabilities:
1. Use `mcp_dart_and_flut_widget_inspector` to inspect the Flutter widget tree and identify navigation/library loading issues.
2. Use `activate_oracle_to_postgres_migration_tools` to verify and configure the Python FastAPI backend for Oracle to PostgreSQL migration.